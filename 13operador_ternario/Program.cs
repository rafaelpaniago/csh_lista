// Vamos ter uma variável de média e retornar a situaçao do aluno.

// Variável
double media = 8;

// Operador ternário
// Console.WriteLine(media >= 7 ? "Aprovado" : "Reprovado");

// Para armazenar essa lógica numa variável
string situacao = media >= 7 ? "Aprovado" : "Reprovado";
Console.WriteLine(situacao);